[workspace]
members = ["crates/emulator-core", "crates/emulator-wasm"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
repository = "https://github.com/huntrontrakkr/pebble-browser-emulator"

[profile.release]
lto = true
opt-level = 3
panic = "abort"
